<Steps>
  <Step>
    ### Install the Claude Agent SDK adapter

    ```bash
    pip install claude-agent-sdk ag-ui-claude-sdk ag-ui-protocol anthropic
    ```

  </Step>
  <Step>
    ### Run through the official AG-UI Claude adapter

    The backend wraps the official `ClaudeAgentAdapter`, exposes backend
    tools through the Claude Agent SDK MCP server, and yields AG-UI events
    back to CopilotKit.

    <DemoCode file="src/agents/claude_agent_sdk_adapter.py" region="claude-agent-sdk-agent-setup" />

  </Step>
</Steps>
